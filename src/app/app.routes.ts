import { Routes } from '@angular/router';
import { DeferExampleComponent } from './deferExample/defer.component';
import { AppComponent } from './app.component';

export const routes: Routes = [];
